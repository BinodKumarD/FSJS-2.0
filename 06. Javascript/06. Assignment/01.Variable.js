// declare variables and assign string, boolean, undefined and null data types, Display all the value with their data type.

// first name is assigned with string value
var firstName = "Binod"; 
console.log(`First name: ${firstName} (Type of firstName: ${typeof firstName})`);

// isLoggedOn is assigned with boolean value
const isLoggedOn = true;
console.log(`Is Loggedin: ${isLoggedOn} (Type of isLoggedOn: ${typeof isLoggedOn})`);

//In lastName we will not be defining any value so by default it will be undefined
let lastName;
console.log(`Last Name: ${lastName} (Type of last name: ${typeof lastName})`);

//age value is assigned with null value
const age = null;
console.log(`Age: ${age} (Type of Age: ${typeof age})`);
