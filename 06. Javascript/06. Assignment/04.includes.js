// Declare a varibale and assign string value to it and check if the string contains a word Script using includes() method

const fullName = "Binod kumar";
let result = fullName.includes("kumar");
console.log(`${result}`);

const textCheck = "Declaring variable and assigning string value to it and using includes function to check the particular part in sentence";
var result1 = textCheck.includes("variable");
console.log(`${result1}`);